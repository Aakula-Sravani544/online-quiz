{
  "name": "quiz-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install && cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node server.js"
  }
}
